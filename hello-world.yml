apiVersion: v1
kind: pod
metadata:
  name: nodehelloworld.example.com
  labels:
    app: helloworld
  spec:
    containers:
      - name: nodetestapp
        image: atishayshukla/nodetestapp
        ports:
          - name: nodejs-port
            containerPort: 3000
      
